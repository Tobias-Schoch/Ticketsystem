# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=sicheres-passwort-hier
POSTGRES_DB=ticketsystem

# JWT Secrets (generieren mit: openssl rand -base64 64)
JWT_ACCESS_SECRET=dein-access-secret-mindestens-64-zeichen-lang-hier-eintragen
JWT_REFRESH_SECRET=dein-refresh-secret-mindestens-64-zeichen-lang-hier-eintragen
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# CORS
CORS_ORIGIN=https://ticket.schoch.studio

# Email (SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@ticketsystem.de
SMTP_FROM_NAME=Ticketsystem

# Admin Account (wird beim ersten Start erstellt)
ADMIN_EMAIL=admin@ticketsystem.de
ADMIN_PASSWORD=SicheresAdminPasswort123!
ADMIN_NAME=Administrator
